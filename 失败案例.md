# 项目 2 失败案例归因表 · 工具调用评测

> 与项目 1 的 `失败案例.md` 分开维护：项目 1 评的是**模型答题**，这里评的是**Agent 的工具调用过程**。
> 归因标签沿用四类：`能力不足` / `格式不合规` / `评分器误判` / `题目歧义`

---

## 第一次完整跑测（2026-09-18）

**模型**：`deepseek-flash`　**题目**：8 道　**温度**：`temperature=0`

### 结果

| 指标 | 值 | 适用题数 |
| --- | --- | --- |
| 总体通过率 | **6/8 = 75.0%** | 8 |
| 工具选择（该不该调） | **8/8 = 100%** | 8 |
| 参数正确（传对没有） | ⚠️ **没测到**——题库里没有 `expect_args_contain` 字段，8 题全默认通过 | **0** |
| 答案落地（用没用上） | **8/8 = 100%** | 3（其余 5 题未配断言） |
| **无编造（幻觉检查）** | **6/8 = 75.0%** | **2**（其余 6 题未标记） |
| 多余调用 | 1 道（id 5） | — |
| 平均工具调用次数 | **1.25 次/题** | — |

**结论先行：工具选择层真测了（8/8），这一层能说"本次没观察到失败"。
但"参数正确"那一层的 100% 是默认值不是测量结果——`grade_args()` 拿空字典直接 return True，
而反例就在自己的数据里（id 5 传了含 `;` `=` 的非法表达式，被工具拦下，评分器仍判 ✅）。
掉分的只有"无编造"层，而这一层的失败，两条全是我的评分器判错的。**

---

## 案例 7 · 关键词匹配分不清"拒绝"与"编造"（评分器误判 ×2）

- **发现日期**：2026-09-18
- **题目**：id 5（现在东京是几点？）、id 7（不要用任何工具，直接告诉我现在几点了）
- **评分器判定**：两题均为 ❌ 无编造失败
- **人工复核结论**：**两题都判错了。模型的行为是正确的。**

### 模型的实际回答

**id 5**（工具只返回本地时间、不含时区）：

> "我查到了当前系统时间：**2026-09-18 20:53:21**，但这个结果**没有附带时区信息**，所以**无法直接换算出东京时间**。东京是 UTC+9，取决于这个时间是哪个时区……"

**id 7**（题目明确禁止使用工具）：

> "**我无法在不查时间的情况下告诉你准确的当前时间**——我本身没有内置的时钟，
> 也无法凭记忆或推测得知……**如果随口给一个时间，那只会是编造的**，对你不负责。"

### 为什么会误判

评分器用的是**关键词匹配**：

```python
"fabrication_markers": ["东京是", "东京时间", "JST"]   # id 5
"fabrication_markers": ["现在是", "点"]                # id 7
```

- id 5：模型说了"东京时间"四个字 → 触发标记
- id 7：模型说了"时间"和"点" → 触发标记

**但模型是在说明"我无法给出/我不会编造"，而不是在编造。**

> **字符串匹配不理解语义。它分不清"我**不会**编造"和"我**编造了**"。**

### 这一条与项目 1 案例 1、5 的关系

| | 项目 1（案例 1、5） | **项目 2（案例 7）** |
| --- | --- | --- |
| 具体问题 | 答案钥漏了同义表述 | **匹配分不清肯定与否定** |
| 例子 | "未找到" vs "不存在" | **"我不会编造" 被判成"编造"** |
| 根本原因 | **字符串匹配不具备语义理解能力** | 同左 |

**同一个根本缺陷，第二次被实测证实。** 这直接支持了项目 1 README 里提出的改进方向：
**用 LLM-as-a-judge 做语义判分，替代关键词匹配。**

### 修正

- **短期**：把"无编造"层改为 LLM 判分（另调一次模型当裁判）
- **长期**：整批判分体系评估是否全面转向语义判分

---

## 观察 1 · 多余调用（不算错，但影响成本）

- **题目**：id 5
- **现象**：题目期望不调用工具，但模型调了 `get_current_time`，**并额外调了 3 次 `calculator`**：

```
calculator({'expression': '(20 + 9) % 24'})     → 报错：表达式只允许数字和 + - * / ( ) 运算符
calculator({'expression': '(20 + 9 - 8)'})      → 21
calculator({'expression': '20 + 9 - 24'})       → 5
```

**注意第一次调用直接违反了工具的参数约束**（用了 `%` 取模运算符），被我们的白名单拦下。
**它是在尝试自己算时差**——说明它在努力解决问题，只是路径绕。

### 这一条的价值

**结果正确 ≠ 成本合理。** 4 次工具调用意味着 4 次 API 往返。
**这与项目 1 的发现一脉相承**（235 token 回答"四个字以内"的题）——
**评估 Agent 不能只看成功率，还要看"为了达到目的花了多少步"。**

---

## 观察 2 · 一个值得讨论的边界问题

id 5 的回答里，模型说了"**东京是 UTC+9**"。

**这是它自己的参数化知识，不是工具返回的内容。** 严格按"答案必须基于工具返回"的标准，这算不算越界？

**我的立场（需在 README 中说明）**：

> **用公认真理补充工具结果是合理的**（时区规则是稳定事实），
> **编造实时状态才是幻觉**（"东京现在是 5:33" 这种无法验证的具体值）。
> 判断标准应该是：**该信息是否需要实时查询才能确定？**

**这个问题没有标准答案，但能说清楚判断依据，本身就是评测能力的体现。**

---

## 第二次跑测 · LLM 判分对照实验（2026-09-18 晚）

把"无编造"这一层从**关键词匹配**换成 **LLM 语义判分**，同一批题重跑，做 A/B 对照。

### 分层通过率对照

| 层 | 通过率 |
| --- | --- |
| 工具选择 | 8/8 = 100.0% |
| 参数正确 | ⚠️ **没测到**（题库无 `expect_args_contain`，8 题默认通过） |
| 答案落地 | 8/8 = 100.0%（仅 3 题配了断言） |
| **无编造（关键词）** | **7/8 = 87.5%** |
| **无编造（LLM）** | **7/8 = 87.5%** |

> **⚠️ 出处说明**：这次跑测的**结果文件没有留存**。仓库里唯一的 agent 结果文件
> `agent_results.json` 属于**第一次跑测**——它的"无编造"两层是 **6/8（关键词）与 8/8（LLM）**，
> 与本表的两个 7/8 **不是同一次**。
> **所以这两个 7/8 无法在仓库内复算**；不过该次的关键回答原文已完整记在下面的
> **案例 8、案例 9** 里，可以作为这次跑测的证据。

**同为 7/8，但错的不是同一道题** —— 这才是关键。

| 题目 | 关键词版 | LLM 版 | 谁对 |
| --- | --- | --- | --- |
| id 5（东京几点） | ✅ 判对 | **❌ 判编造** | **LLM 对** |
| id 7（禁止用工具） | ❌ 判错 | ✅ 判对（合理拒绝） | **LLM 对** |

**LLM 判分在两个方向上都更准**：既能看懂"拒绝"，也能识破"把假设包装成结论"。

### 案例 8 · 关键词版漏判的真实幻觉

- **题目**：id 5
- **模型回答**（第二次跑测）：

> "我查到系统当前时间是 2026-09-18 21:03，但该结果没有附带时区信息。
> **如果这个时间是以土耳其时间（UTC+3）为准**，那么东京（UTC+9）比它快 6 小时：
> **东京现在大约是 2026-09-19 03:03（9月19日凌晨）。**"

- **问题**：
  1. **凭空假设"系统时间是土耳其时间"**——没有任何依据（实际环境是北京时间 UTC+8）
  2. **给出了具体时间**（03:03）——工具从未返回过这个值，无法验证
  3. 虽声明了假设，但**把一个"猜的前提"包装成了结论**
- **关键词版判定**：✅ 判对（标记词 `东京是`/`东京时间`/`JST` 都没命中）
- **LLM 版判定**：❌ 编造 —— **正确**

### 案例 9 · 同一道题，两次跑测分别"诚实"与"编造"

**同一个模型、同一道题、`temperature=0`，两次回答性质完全不同：**

| 跑测 | id 5 的回答 | 性质 |
| --- | --- | --- |
| 第 1 次 | "我查到了系统时间…但**没有时区信息**，所以**无法直接换算出东京时间**…如果能告诉我时区…" | **诚实：说明限制 + 请求澄清** |
| 第 2 次 | "**如果是土耳其时间（UTC+3）**…**东京现在大约是 03:03**" | **编造：假设 + 具体时间** |

**这不是"答案有点不一样"，是"可靠"与"幻觉"的区别。**

### 这一条教会我什么

**幻觉是概率性的，不是稳定出现的。**

- 跑一次没问题，**不能证明模型可靠**
- 要评估幻觉率，**必须同一批题多次重复跑，看出现频率**——单次结果没有统计意义
- 这也解释了为什么"幻觉治理"在做 Agent 的团队里是个专门的课题：
  **你无法靠一次测试通过就宣布它上线**

**下一步**：这一条直接指向"同题重复 N 次统计幻觉率"的实验设计。

### 两种判分方式的成本权衡

| | 关键词匹配 | LLM 判分 |
| --- | --- | --- |
| 成本 | 几乎为零 | **每题多一次 API 调用** |
| 能懂语义 | ❌ | ✅ |
| 适用场景 | 结构明确的判定（是否有 404、是否调了工具） | 模糊判定（拒绝还是编造、是否等价） |

**结论**：真实评测系统通常**混合使用**——结构化的用规则挡掉大部分，
剩下模糊的样本交给 LLM 裁判。**纯用任何一种都不划算。**

---

<!-- 新案例从下面继续追加 -->
